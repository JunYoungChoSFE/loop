# 기획서 — **Loop** (가칭): 소상인용 정직한 Shopify 로열티 앱

> 가칭 "Loop" = 재구매 루프(loop)에서 따옴. 대체안: *Plainloop, Loyable, Honestly Loyal*. 출시 전 자유롭게 변경.
> 이 문서는 사람이 읽는 기획서다. Claude Code가 읽는 건 `CLAUDE.md`, 빌드 순서는 `02_BUILD_PROMPTS.md` 참조.

---

## 1. 한 줄 정의

복잡함과 약탈적 가격에 지친 **소규모 Shopify 상인**을 위한 — 5분 세팅, 단일 정액, 다크패턴 0의 포인트·추천(referral) 로열티 앱.

## 2. 문제 (실제 리뷰에서 검증된 불만)

Smile / Joy / Rivo 등 상위 로열티 앱의 ★1~3 리뷰에서 반복적으로 나온 4대 불만:

1. **약탈적·복잡한 가격** — 기본 기능이 상위 플랜에 잠겨 있고, 주문 수 기반 과금이라 가게가 성장할수록 벌받는 구조. API·핵심 기능이 월 $600~$1,000 플랜 뒤에 있음. 소상인은 "이건 우리용이 아니다"라고 느낌.
2. **고객용 위젯/포털이 별로** — 별도 팝업 포털이 고객을 헷갈리게 하거나, 랜딩 페이지가 기본 상태에서 정렬·색상·패딩이 깨져서 커스텀 CSS 없이는 못 씀.
3. **다크패턴** — 기능 잠금 해제를 위한 별 5개 강요, 인앱 업셀 팝업, 호환된다고 표시했지만 작동 안 하는 연동, **앱 삭제 후에도 남는 코드**.
4. **지원이 유료 게이팅** — "커스텀 CSS가 필요하다"고 떠넘기거나, 도와주려면 상위 플랜으로 업그레이드하라고 함.

→ **이 4개를 정면으로 뒤집는 게 제품의 전부다.**

## 3. 타깃 고객

월 주문 수십~수백 건 규모의 **소규모 Shopify 상인**. 직접 가게를 운영하며 시간이 없고, 비싼 엔터프라이즈 로열티 플랫폼을 감당할 수 없고, 코드를 못 만지는 사람. (강자들이 명시적으로 버린 세그먼트.)

## 4. 차별점 (Wedge) — 기능이 아니라 **포지셔닝**

> ⚠️ "기능 적은 버전"은 망한다. 우리의 wedge는 적은 기능이 아니라 **정직함 + 단순함 + 적정 가격**이다. 핵심 일(로열티)은 완결되게 한다.

| 강자들의 불만 | Loop의 답 |
|---|---|
| 약탈적/복잡한 가격, 주문 수 페널티 | **단일 정액제. 주문 수 페널티 없음. 페이월 없음.** |
| 깨지는 위젯, 커스텀 CSS 강요 | **코드 0으로 처음부터 예쁜 on-brand 위젯.** |
| 다크패턴 | **리뷰 강요 0, 인앱 업셀 0, 삭제 시 코드 완전 제거.** |
| 지원 유료 게이팅 | **모든 플랜에 빠른 인간 지원.** |

대기업은 구조적으로 이걸 못 한다(투자받았으니 업셀해야 함). **솔로 개발자는 단순함을 유지하는 게 오히려 유리한 거의 유일한 위치.**

## 5. v1 핵심 기능 (목표 4주)

완결된 로열티 프로그램에 필요한 최소한, 그러나 빠짐없이:

- **적립(Earn)**: 구매 시 포인트(상인이 적립률 설정), 계정 생성 시 보너스, (선택) 생일 보너스.
- **교환(Redeem)**: 포인트 → Shopify 할인 코드 자동 발행.
- **스토어프론트 위젯**: Theme App Extension 기반. 인라인 런처 + 패널. **별도 팝업 포털 강요 안 함.** 상인 브랜드 색상에 맞춰 코드 없이 자동으로 깔끔하게.
- **추천(Referral)**: 고객별 추천 링크 → 추천인·피추천인 둘 다 보상.
- **어드민(Admin)**: Polaris 대시보드 — 적립 규칙, 리워드, 멤버 목록, 기본 통계.
- **이메일 알림**: 포인트 적립 / 리워드 사용 가능 알림(기본).
- **결제**: Shopify Billing API. Free + 단일 유료 플랜.
- **클린 언인스톨 + GDPR**: 삭제 시 데이터·주입 코드 완전 제거. 필수 웹훅 처리.

## 6. 명시적 비포함 (v1에서 의도적으로 뺌)

VIP 티어, 고급 분석/리포트, 다국어, 게임화, POS 연동, 외부 마케팅 툴 딥 연동. — **좁게 가는 게 전략이다.** 요청이 쌓이면 v2.

## 7. 가격 (정직 모델)

| 플랜 | 가격 | 내용 |
|---|---|---|
| **Free** | $0 | 진짜 쓸 만한 무료 — 핵심 적립·교환·위젯 포함. (월 주문 N건까지, N은 넉넉하게) |
| **Pro** | **$19/월 flat** | 전 기능. 주문 수 페널티 없음, 숨은 비용 없음. |

끝. 티어 사다리 없음. (참고: Shopify 수수료는 연 매출 $1M까지 0%, 이후 15%.)

## 8. 성공 지표

설치 수 · 14일 내 활성화율(적립 1건 이상 발생) · Free→Pro 전환율 · App Store 평점(정직하게 쌓음) · 30일 가맹점 리텐션.

## 9. 리스크 (정직하게)

- 카테고리가 붐비고 강자(Smile 10만+ 가맹점)가 있음.
- App Store 검색 순위는 리뷰가 쌓여야 올라가서 초반 노출이 느림.
- 강자가 가격을 내리면 흔들릴 수 있음.
- **베팅의 전부 = 버려진 소상인 세그먼트 + 정직 포지셔닝.** 슬램덩크 아님. 단, 수백 명의 소상인이 불만을 이미 글로 써둔, 근거 있는 출발.

## 10. 출시 전 마지막 검증 (인맥 0이어도 가능)

빌드 시작 전, r/shopify · Shopify Community 포럼 · 인디해커 커뮤니티에 가서 소상인에게 직접 물어본다: "로열티 앱 뭐 써요? 제일 불편한 게 뭐예요?" 위 4대 불만이 거기서도 똑같이 나오면 → 빌드 GO.
