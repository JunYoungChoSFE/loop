# 보고서 — 당신(사람)이 해야 할 일

> 대상: Loop 프로젝트 오너 (liger4903@gmail.com)
> 작성일: 2026-06-07
> 범위: LOOP 시스템에서 **사람만 할 수 있는 일**만 추림. 코드 작업은 Claude Code 몫이라 제외.
> 원칙: 아래 일들은 내가(Claude) 대신 못 한다 — 계정·결제·실제 스토어 검증·대화형 로그인은 구조적으로 당신 손이 필요하다.

---

## 0. 한눈 요약 (현재 상태)

| 구분 | 항목 | 상태 |
|---|---|---|
| 환경 | Node 18+ | ✅ v24.14.1 |
| 환경 | npm | ✅ 11.11.0 |
| 환경 | git | ✅ 2.53.0 |
| 환경 | Shopify CLI | ✅ 글로벌 설치 완료 (v4.x) |
| 환경 | Postgres/Docker | ➖ 불필요 (로컬은 SQLite) |
| 계정 | Shopify Partner 계정 | ❓ **확인 필요** |
| 계정 | 개발 스토어 | ❓ **확인 필요** |
| 검증 | 4대 불만 시장 검증 | ⬜ 권장(미완) |

**다음 한 걸음:** Partner 계정 + 개발 스토어 준비 여부만 확정하면 Phase 0 시작 가능.

---

## 1. 🔴 빌드 시작 전 (블로커 — 없으면 진행 불가)

> ⚠️ **2025년 변경:** Shopify가 개발자 진입점을 기존 Partner Dashboard(`partners.shopify.com`)에서
> 신규 **Dev Dashboard(`dev.shopify.com`)** 로 옮겼다. 아래는 최신 절차 기준.
> (둘 다 당분간 공존하지만, 새로 시작하면 Dev Dashboard 권장.)

### 1-A. 개발자 계정 생성 (무료, ~3분)

| 순서 | 할 일 |
|---|---|
| 1 | **`dev.shopify.com` 접속 → "Sign up" / 로그인** (이메일 또는 Google·Apple 소셜 로그인) |
| 2 | 안내에 따라 개발자 조직(Organization) 생성 — 이름·국가 입력만 하면 끝 |

> 대안: 기존 Partner 프로그램을 쓰고 싶으면 `partners.shopify.com` 가입도 여전히 유효. CLI 로그인 시 둘 중 어느 계정이든 인식됨.

### 1-B. 개발 스토어 1개 생성 (무료·무제한, ~3분)

| 순서 | 할 일 |
|---|---|
| 1 | Dev Dashboard 좌측 사이드바 → **Stores** → **Create store** |
| 2 | **스토어 이름** 입력 (자동으로 `xxx.myshopify.com` URL 생성됨) |
| 3 | **플랜 티어 선택** → 개발용은 **Basic**이면 충분 (어차피 개발 스토어는 무료·실결제 불가) |
| 4 | (선택) feature preview 켜기 → 그냥 두고 **Create store** |

> 레거시 경로(Partner Dashboard 쓸 경우): Stores → Add store → **Create development store** → 용도는 **"test and build"** 선택.

### 1-C. 나머지

| # | 할 일 | 상태 |
|---|---|---|
| 1 | ~~Shopify CLI 글로벌 설치~~ | ✅ **완료** (`npm i -g @shopify/cli` 끝남) |
| 2 | **(권장) 시장 검증** — r/shopify·Shopify Community에 "로열티 앱 뭐 써요? 제일 불편한 게?" → 4대 불만 재현되면 GO (기획서 10절) | ⬜ |

> **핵심 블로커 = 1-A 계정 + 1-B 개발 스토어.** 이 둘이 없으면 Phase 0 중간 `shopify app dev`의 계정 로그인·스토어 선택 단계에서 멈춘다.
> 참고: Shopify CLI는 **기존 개발 스토어에 연결**만 한다 — 스토어 자체는 위 1-B에서 당신이 먼저 만들어 둬야 한다.

---

## 2. 🟡 빌드 진행 중 (매 Phase 반복 — 내가 못 하는 일)

| # | 할 일 | 언제 |
|---|---|---|
| 5 | **다음 Phase 지시** ("Phase 1 진행해" 정도면 됨 — 02 문서는 내가 이미 읽음) | 매 Phase 시작 |
| 6 | **대화형 명령 실행** (`shopify app dev`, OAuth 브라우저 승인 등) — 프롬프트에 `! 명령` 형태로 | Phase 0~ |
| 7 | **비밀값 입력** (`SHOPIFY_API_KEY`, `SHOPIFY_API_SECRET`, `DATABASE_URL`) — 커밋 금지 | 설정 시 |
| 8 | **개발 스토어 실제 동작 확인** (LOOP의 철칙) | 매 Phase 후 |

### Phase별 "당신이 직접 확인할 것"
- **Phase 1**: 개발 스토어에서 테스트 주문 생성 → 포인트 적립되는지
- **Phase 2**: 어드민 설정 화면이 5분 안에 완료되는지
- **Phase 3**: 교환 시 할인 코드가 실제 발행되는지
- **Phase 4**: 테마에서 위젯이 안 깨지고 브랜드 색으로 뜨는지
- **Phase 5**: 추천 링크로 첫 구매 시 양쪽 적립되는지
- **Phase 7**: Pro 구독 결제 플로우(테스트 과금)가 도는지
- **Phase 8**: 설치→적립→교환→추천→삭제 전체 플로우 + **삭제 후 가게에 흔적 0** 확인

---

## 3. 🟢 출시 단계 (Phase 8 이후 — 전적으로 당신 몫)

| # | 할 일 | 비고 |
|---|---|---|
| 9 | **호스팅 배포 결정·계정** | Fly.io 또는 Render |
| 10 | **App Store 리스팅 작성** | 스크린샷, 데모 스토어 자격증명, 지원 이메일 |
| 11 | **앱 심사 제출** | Partner 대시보드. 5~10영업일, 첫 제출은 보통 수정 요청 |
| 12 | **지원 이메일 운영** | 기획서 4절: 모든 플랜에 빠른 인간 지원 = 차별점 |

---

## 4. 지금 당장 (Action Item)

1. **Partner 계정·개발 스토어 준비 여부를 Claude에게 알려주기**
   - 됐으면 → "Phase 0 시작" 한마디면 내가 스캐폴딩 착수
   - 아직이면 → 위 1·2번부터 (각 5분)
2. (선택) Shopify CLI 글로벌 설치: `! npm i -g @shopify/cli@latest`

---

## 부록 — 역할 경계 (왜 이건 당신 일인가)

| 일 | 주체 | 이유 |
|---|---|---|
| 코드 작성·테스트·구조 설계 | **Claude** | 자동화 가능 |
| 계정/스토어 생성, 결제, OAuth 승인 | **사람** | 인증·신원·금융 — 위임 불가 |
| 실제 스토어에서 눈으로 동작 확인 | **사람** | 내가 브라우저로 가맹점 화면을 볼 수 없음 |
| App Store 제출·심사 대응 | **사람** | Shopify 정책상 오너 계정 행위 |
