# 14 — 심사 제출 실행 가이드 (내가 직접 할 일 + 영상 제작)

> 이 문서는 **사람(나)이 손으로 해야 하는 행동만** 순서대로 모은 실행 체크리스트다.
> 코드/웹훅/엔진 구현은 이미 끝났다(테스트 68개 통과). 여기서는 "내가 클릭하고, 입력하고,
> 촬영하고, 제출하는" 것만 다룬다.
> 참고: 복붙용 필드값 `13_SUBMISSION_PACKET.md` · 거절 대비 `12_APP_STORE_REVIEW_GUIDE.md`
> · E2E 9장 `11_DEV_STORE_TEST_GUIDE.md` · 리스팅 원문 `06_APP_STORE_LISTING.md`.

---

# PART 1 — 내가 직접 해야 하는 일 (제출까지)

순서대로 위에서 아래로 진행한다. 각 단계 끝의 **[확인]** 을 통과해야 다음으로 넘어간다.

## STEP 0 — 사전 점검 (10분, 터미널)

> 코드는 이미 준비됨. 내가 "지금 실제로 도는지"만 확인한다.

```powershell
# 1) 테스트 전부 통과하는지
npm test                # 11 files / 68 tests 통과여야 함

# 2) 타입 깨진 데 없는지
npm run typecheck

# 3) toml(scopes·webhooks·proxy)을 Shopify에 푸시 — fly 배포와 별개다!
shopify app deploy
```

- **[확인]** `npm test` 초록불, `shopify app deploy` 가 새 app version 을 만들고 성공.
- ⚠️ `shopify app deploy` 를 빼먹으면 어드민 코드는 새거인데 Shopify 쪽 웹훅/scope 설정은
  옛날 거라 심사관 환경에서 안 맞을 수 있다. **제출 전 반드시 한 번.**

## STEP 1 — 프로덕션이 실제로 떠 있는지 (5분, 브라우저)

심사관은 터널 URL이 아니라 `application_url` = `https://loop-loyalty.fly.dev` 로 접속한다.

1. 시크릿 창에서 `https://loop-loyalty.fly.dev` 열기 → 200(에러 페이지 X).
2. 안 뜨면 배포 필요: `05_DEPLOYMENT.md` 절차대로 `fly deploy` 후 다시 확인.
3. (fly 로그를 띄워두면 심사 중 설치/웹훅을 실시간으로 볼 수 있다: `fly logs`)

- **[확인]** 비로그인 시크릿 창에서 fly URL 이 200.

## STEP 2 — 개인정보처리방침 URL 살아있는지 (2분, 브라우저)

1. 시크릿 창에서 열기:
   `https://github.com/JunYoungChoSFE/loop/blob/main/08_PRIVACY_POLICY_EN.md`
2. 로그인 없이 내용이 보이면 OK (repo 가 private 이면 404 → **public 으로 전환** 필요).

- **[확인]** 비로그인 200 + 영문 개인정보방침 본문이 보임.

## STEP 3 — PCD(보호된 고객 데이터) 승인 상태 (3분, Partner Dashboard)

> 메모리상 2026-06-08 승인 완료. 제출 직전 한 번 더 눈으로 확인.

1. `partners.shopify.com` → 해당 앱 → **API access requests → Protected customer data access**.
2. **Email** 필드 + data protection details 설문이 **Approved/Granted** 인지 확인.
3. 아직이면 신청 + 설문 작성 (근거 문서: `07_SECURITY_INCIDENT_POLICY.md`, `08_PRIVACY_POLICY*.md`).

- **[확인]** PCD Approved. (이게 안 되면 프로덕션에서 `orders/create` 적립이 안 들어온다.)

## STEP 4 — 심사용 개발 스토어에 데모 데이터 시드 (5분, 터미널)

심사관이 빈 화면을 보면 "기능을 못 찾음"으로 거절난다. 미리 채워둔다.

```powershell
# 심사용 스토어 도메인으로 (기본값: loop-yxvyj2nb.myshopify.com)
node scripts/seed-demo.mjs <심사스토어>.myshopify.com
```

1. 앱 어드민 → **대시보드/멤버/예측** 화면에 데이터가 보이는지 확인.
2. **설정 → 리워드**에 최소 1개 있는지 (예: "$5 off / 500pt"). 없으면 추가 — 교환 시연에 필요.
   - 데모 정리는 `node scripts/seed-demo.mjs --clean <스토어>`.

- **[확인]** 대시보드·예측 화면이 비어있지 않고, 교환 가능한 리워드 1개 이상.

## STEP 5 — 개발 스토어 E2E 9장 (30~45분, `11_DEV_STORE_TEST_GUIDE.md`)

제출 전 **필수**. 9장을 직접 손으로 한 번씩 통과시킨다(설치 → 적립 → 위젯 → 교환 → 추천
→ 예측 → GDPR → 클린 언인스톨). 각 장의 **[확인]** 을 모두 ✅ 로 만든다.

- **[확인]** 11번 문서 9장 전부 통과. (여기서 막히면 그게 곧 심사 거절 지점이다 — 먼저 고친다.)

## STEP 6 — 스크린샷 3~5장 (20분, `06_APP_STORE_LISTING.md` §shot-list)

> STEP 4 로 데모가 채워진 상태에서 촬영.

권장 5장: ① 대시보드 ② 설정(5분 세팅) ③ 스토어프론트 위젯(포인트+교환) ④ 예측(신뢰도 표시)
⑤ 플랜(Free + Pro $19).

1. 1280×720 이상, 깨끗한 브라우저(개인정보/북마크 비노출), 실제 고객 이메일 가리기.
2. PNG 로 저장. 리스팅 업로드용 폴더에 모아둠.

- **[확인]** 최소 3장(권장 5장) 확보.

## STEP 7 — 심사 영상 촬영·업로드 (PART 2 참조)

→ 아래 **PART 2** 대로 촬영 → YouTube **Unlisted** 업로드 → 링크 복사.
(영상은 필수는 아니지만 강력 권장 — 심사관 플로우 이해를 도와 통과율을 높인다.)

- **[확인]** YouTube Unlisted 링크 확보.

## STEP 8 — Partner Dashboard 리스팅 작성 (30분, `13_SUBMISSION_PACKET.md` 복붙)

`13` 문서의 값을 **그대로** 붙여넣는다.

| 화면 | 입력값 출처 |
|---|---|
| App listing (이름/태그라인/소개/상세/기능/가격) | `13` §1 (상세·기능 본문은 `06`) |
| Feature video | STEP 7 의 YouTube 링크 |
| Screenshots | STEP 6 의 3~5장 |
| App setup (App URL / redirect / GDPR webhook) | `13` §2 |
| Privacy policy URL | `13` §0 |
| Pricing (Free / Pro $19, Billing API only) | `13` §1 |
| **Test instructions (심사관 노트)** | `13` §3 **전체 복붙** ← 통과율 좌우 |
| Support email | `liger4903@gmail.com` |

- **[확인]** 모든 필드 채움. 특히 Test instructions 에 시드 명령·단계가 들어갔는지.

## STEP 9 — 최종 점검 후 제출 (10분)

`13` §5 / `12` §6 게이트를 마지막으로 훑는다:

```
[ ] fly URL 200 (시크릿)             [ ] PCD Approved
[ ] 개인정보방침 URL 200 (시크릿)     [ ] shopify app deploy 완료
[ ] E2E 9장 통과                      [ ] 스크린샷 3~5장
[ ] 영상 링크                         [ ] Test instructions 복붙
[ ] Billing: Free + Pro $19          [ ] 외부결제 흔적 0
```

→ **Submit for review** 클릭.

## STEP 10 — 제출 후

- 심사 **5~10영업일**. **첫 제출은 거의 수정 요청(Changes requested)** 을 받는다 — 정상.
- 수정 요청이 오면 항목별로 고치고 **"무엇을 어떻게 고쳤는지" 회신에 적어** 재제출.
- 지원 메일(`liger4903@gmail.com`) 24시간 내 응답 가능하게 유지.

---

# PART 2 — 심사 제출용 영상 제작 (상세)

> **규칙: 화면에 들어가는 자막/내레이션은 영어. 내가 촬영하며 하는 행동은 한국어.**
> 심사관은 글로벌 기준이라 영어 자막, 그리고 **음소거로 보는 경우가 많아** 내레이션보다
> **화면 자막(번인)** 이 안전하다. 아래 영어 문장을 그대로 자막으로 굽는다.

## A. 사양

| 항목 | 값 |
|---|---|
| 포맷 | MP4, 1080p (1920×1080), 가로 |
| 길이 | 심사관 워크스루 **2~4분** (리스팅용은 여기서 하이라이트만 잘라 ≤90초) |
| 오디오 | 무음 + **영어 자막 번인** 권장 (또는 영어 내레이션). 저작권 음악 금지 |
| 게시 | YouTube **Unlisted** 업로드 → 리스팅 "Feature video" 에 링크 |

## B. 촬영 전 준비 (내가 하는 행동 — 한국어)

1. 데모 데이터 시드: `node scripts/seed-demo.mjs <스토어>.myshopify.com` (PART 1 STEP 4 와 동일).
2. 리워드 최소 1개 확인(설정 → 리워드, 예 "$5 off / 500pt"). 교환 장면에 필요.
3. 탭 미리 열어두기: Roost **대시보드 · 예측 · 설정**, 위젯 있는 **스토어프론트**, Shopify **주문(Orders)**.
4. **깨끗한 브라우저 창** — 개인 북마크/이메일/실고객 정보 비노출. 데모 데이터만 보이게.
5. 창을 16:9, 글자가 읽히도록 확대(125~150%).
6. 화면 녹화 도구 준비 (Windows: **Xbox Game Bar `Win+G`**, 또는 OBS Studio / ShareX).

## C. 촬영 콘티 (심사관 워크스루, 2~4분)

> **각 행의 "내가 하는 행동"(한국어) 대로 클릭하면서, "화면 자막"(영어) 을 그 장면에 자막으로 넣는다.**
> 클릭이 눈에 보이게 천천히. 한 컷당 4~12초.

| # | 내가 하는 행동 (한국어) | 화면 자막 / On-screen caption (영어) |
|---|---|---|
| 1 | dev 스토어 어드민에서 Roost 앱을 클릭해 임베디드로 연다. 로딩되는 모습. | `Roost is an embedded app using session-token auth — no cookies.` |
| 2 | 설정 화면을 연다. 적립률(예: $1 = 1pt) 지정, 리워드 "$5 off / 500 pts" 추가, **저장** 클릭. | `Setup is one short screen: an earn rate and a reward. Five minutes, no code.` |
| 3 | Shopify **주문(Orders)** 으로 가서 고객이 있는 주문을 하나 생성(또는 초안 주문을 결제 완료로). | `I'll place a test order for a customer.` |
| 4 | Roost **대시보드** 로 돌아온다. 최근 활동에 새 "+points" 행, **멤버** 열어 그 고객 잔액 표시. | `The orders/create webhook awarded points — shown on the dashboard and the member's balance.` |
| 5 | **스토어프론트** 로 가서 로그인 고객으로 Roost 위젯을 연다. 리워드 선택 → **교환(Redeem)** 클릭. 발급된 1회용 할인 코드 표시. | `The customer redeems points for a single-use Shopify discount code, issued via the Admin API.` |
| 6 | **추천(Referrals)** 화면을 연다. 추천 링크와, 완료된 추천이 양쪽에 보상된 내역 표시. | `Referrals reward both the referrer and the referred customer once, with self-referral guards.` |
| 7 | **예측(Predictions)** 화면을 연다. 임박(🔁)/이탈 위험(⚠️) 요약·목록을 보여주고, **"기본 OFF" 동의 토글**과 **신뢰도(basis) 컬럼**을 마우스로 가리킨다. | `Predictions estimate repurchase timing and churn risk. Every automatic action is opt-in and OFF by default, and each prediction shows its confidence.` |
| 8 | **플랜(Billing)** 화면을 연다. Free + Pro $19 표시 → 업그레이드 클릭해 **Shopify Billing 확인 화면**까지만 보여줌(결제 완료 X). | `Billing uses the Shopify Billing API only — no external payment processors.` |
| 9 | 설정 화면이나 메모로 GDPR 웹훅 3종이 구현됨을 짧게 가리킴(customers/data_request · customers/redact · shop/redact). | `The three mandatory GDPR compliance webhooks are implemented.` |
| 10 | 스토어에서 **앱 삭제(Uninstall)** → 스토어프론트를 새로고침해 **위젯이 사라진 것**을 보여줌(주입 스크립트·데이터 잔여 0). | `On uninstall, Roost removes its data and storefront blocks — zero trace.` |

> 마지막 컷(선택): 대시보드로 복귀 + 카드 자막 `Clean uninstall · GDPR · zero dark patterns. That's Roost.`

## D. 리스팅용 짧은 버전(선택, ≤90초)

위 워크스루에서 다음 컷만 잘라 붙이면 마케팅용이 된다: **2(설정) → 5(위젯 교환) →
7(예측) → 8(플랜)**. 각 컷에 `06_APP_STORE_LISTING.md` 의 한 줄 카피를 자막으로.

## E. 촬영 후 (내가 하는 행동 — 한국어)

1. 트리밍 후 **영어 자막을 번인**(태우기) — 위 C 표의 영어 문장 그대로.
2. 1080p MP4 로 내보내기.
3. YouTube **Unlisted** 업로드 → 링크 복사.
4. Partner Dashboard 리스팅 → **Feature video** 에 링크 붙여넣기 (스크린샷은 별도 필수 — STEP 6).
5. dev 에서 촬영했다면 `shopify app dev clean` 으로 프로덕션 URL 복귀.

- **[확인]** Unlisted 영상이 시크릿 창에서 재생되고, 자막이 영어로 보임.
