# 기획서 — **Loop v2: 재구매·이탈 예측 엔진**

> 이 문서는 Loop v1(정직한 로열티 앱) 위에 얹는 **예측 기능 v2**의 개발 기획서다.
> 읽는 주체: 사람(설계 이해용) + Claude Code(빌드용).
> 짝 문서: 기존 `CLAUDE.md`에 이 문서의 **9. 가드레일**과 **6. 데이터 모델**을 병합한다.
> 한 줄 마인드셋: **"예측 숫자를 파는 게 아니라, 그 예측이 일으키는 액션을 판다."**

---

## 0. 핵심 결정 요약 (먼저 읽을 것)

| 항목 | 결정 | 이유 |
|---|---|---|
| 예측 대상 | "이탈 yes/no" ❌ → **P(살아있음) + 다음 구매 예상시점** ✅ | 이커머스는 비계약형. 해지 버튼이 없어 churn 라벨을 못 만든다 |
| 모델 | GBM 분류기 ❌ → **BG/NBD + Gamma-Gamma** ✅ | 상점당 고객 수백 명이라 지도학습 라벨이 부족. 확률모델은 RFM만으로 적합 |
| 콜드스타트 | "데이터 쌓일 때까지 대기" ❌ → **공개데이터 prior로 첫날부터 작동** ✅ | 신규 상인도 즉시 가치. 자체 데이터 쌓이면 자동 개인화 |
| 서빙 | 실시간 ML 서빙 ❌ → **야간 배치 1회** ✅ | 솔로 개발 운영 부담 최소화. 로열티 트리거는 분 단위 정밀도 불필요 |
| 출시 순서 | BG/NBD 먼저 ❌ → **휴리스틱(v2.0) 먼저, BG/NBD(v2.1) 나중** ✅ | 휴리스틱이 가치의 70%를 잡음. 빠른 출시·검증 우선 |

---

## 0.5. 포지셔닝 — 예측은 "차별점"이 아니라 "기본기"다

**현실 직시(2026 시장):** 재구매·이탈·CLV 예측은 이미 흔하고 일부는 공짜다.
- Shopify가 RFM 기반 예측 지출 등급을 **전 플랜에 기본 포함**한다.
- Klaviyo는 다음 주문 시점·이탈 위험·LTV를 예측하고, 대부분의 스토어가 이미 깔아둔 이메일/SMS 채널까지 소유한다.
- Lifetimely·RetentionX·Saras 등 전용 분석앱이 저가로 같은 예측을 제공한다.

따라서 **예측 정확도(BG/NBD 등)는 "앞서가기"가 아니라 "따라잡기" 기능이다.** 이걸 주력 차별점으로 내세우면 Shopify 기본기능·Klaviyo에 정면으로 밀린다. 예측은 헤드라인이 아니라, **정직하고 단순한 로열티 앱에 자연스럽게 딸려오는 편의 기능**으로 포지셔닝한다.

**그럼 Loop의 진짜 경쟁력은 어디인가 (이게 우선순위다):**

1. **통합·단순함.** Loop의 가치는 예측 정확도가 아니라, *로열티 메커니즘 안에서 토글 하나로 닫힌다*는 점이다. Klaviyo + 분석앱 + 로열티앱 3개를 엮기 싫은 영세 상인에게, 한 앱에서 적립·예측·넛지가 자동으로 도는 것이 핵심 가치다.

2. **Klaviyo 스택이 없는/원치 않는 세그먼트.** 월 $150~200짜리 마케팅 스택을 감당 못 하거나 안 쓰는 소상인 — Loop v1이 노린 "대기업·복잡한 앱이 구조적으로 못 가는 자리" 그대로다. 예측 기능은 이 세그먼트가 비싼 스택 없이도 기본적인 재구매 유지를 하게 해준다.

3. **정직·다크패턴 0·플랫 요금.** 진짜 해자는 이 정체성이다. 예측 기능도 이 원칙 안에서만 작동한다(가짜 긴박감 금지, 자동액션 기본 OFF).

**예측 기능이 실제로 주는 실무 가치(과장 없이):**
- 고객이 아직 활성일 때 이탈 신호를 줘 *사후 수습이 아닌 사전 유지*를 돕는다.
- 아무 때나가 아니라 *재구매 시점에 맞춘 한 번의 넛지* → 발송 피로·비용 감소.
- 분석가 없는 소상인이 토글 하나로 기본 분석을 누린다.

> 요약: v1이 "포인트 적립"을 판다면, v2는 그 위에 "재구매 유지 자동화"를 **단순함·정직함이라는 Loop의 본 무기를 강화하는 방식으로** 얹는다. 예측은 그 약속을 받쳐주는 기본기이지, 그 자체가 경쟁우위는 아니다. 상위 플랜 과금을 한다면 근거는 "더 정확한 예측"이 아니라 "한 앱에서 끝나는 자동화·단순함"이어야 한다.

---


소상인 이커머스는 **비계약형(non-contractual)**이다. 고객은 "해지"하지 않고 그냥 조용히 안 온다. 따라서 두 개의 연속량을 예측한다.

1. **P(alive)** — 이 고객이 아직 활성 고객일 확률 (0~1)
2. **expected_next_purchase_date** — 다음 구매가 예상되는 시점

부가로:
- **predicted_clv** — 향후 N개월 기대 매출 (Gamma-Gamma)
- **risk_flag** — 본인 평균 구매주기의 1.5배를 초과하면 "이탈 위험"

> 라벨 기반 분류를 쓰지 않는다. RFM(Recency / Frequency / Monetary)만으로 확률을 적합한다.

---

## 2. 모델 설계

### 2.1 메인 모델: BG/NBD (Beta-Geometric / NBD)

비계약형 재구매 예측의 학계 표준. 고객별 입력 3개:
- `frequency` — 반복구매 횟수 (첫 구매 제외)
- `recency` — 첫 구매 ~ 마지막 구매까지의 기간
- `T` — 첫 구매 ~ 관측시점까지의 기간(고객 나이)

출력: **P(alive)**, **향후 t기간 기대 구매횟수**.

### 2.2 금액 모델: Gamma-Gamma

거래당 평균 금액을 모델링(빈도와 독립 가정). BG/NBD 기대 횟수 × Gamma-Gamma 기대 금액 = **CLV**.

### 2.3 구현 라이브러리

- 1차: Python `lifetimes` (`BetaGeoFitter`, `GammaGammaFitter`) — 가장 간단.
- 권장: `pymc-marketing`의 CLV 모듈 — `lifetimes`는 유지보수가 거의 멈춰서, 신규 코드는 이쪽이 안전.
- **스택 주의**: 메인 앱은 Remix(Node)인데 모델은 Python이다. → **별도 Python 배치 워커**로 분리하고, 같은 DB에 점수만 기록한다(아래 5장).

---

## 3. 콜드스타트 → 개인화 레이어

기다리지 않는다. 데이터 양에 따라 신호 소스를 단계적으로 교체한다.

| 단계 | 조건 | 신호 소스 | 출력 정확도 |
|---|---|---|---|
| L0 | 신규 상점 / 신규 고객 | **카테고리 prior** (소모품 ~30일, 의류 ~90일 등, 공개데이터서 추출) | 대략 |
| L1 | 고객 구매 1회 | 상점 전체 평균 재구매 주기 | 보정됨 |
| L2 | 고객 구매 2회+ | **그 고객 본인의 구매간격 분포** | 개인화 |
| L3 | 상점 활성고객 ≥ 임계치(예: 200) | **BG/NBD 적합** (상점 단위) | 확률모델 |

- 카테고리 prior는 공개데이터로 사전 산출해 앱에 **상수 테이블로 내장**(2.4에서 갱신).
- 각 고객 레코드에 현재 어느 레이어로 산출됐는지 `prediction_source` 기록 → 디버깅·신뢰도 표시용.

### 3.1 콜드스타트용 공개 데이터셋

- **UCI Online Retail II** — 영국 이커머스 트랜잭션 ~100만 건. RFM·재구매 라벨 추출 용이. BG/NBD 기본 prior 적합용.
- **Olist (Brazilian E-Commerce, Kaggle)** — 주문·고객·리뷰 9만+ 건, 카테고리 다양 → 카테고리별 주기 prior.
- **Instacart Market Basket** — 재구매 300만 주문, "다음에 다시 살 품목" 설계 → 소모품 재구매 주기 검증.
- (분류 베이스라인 비교용으로만) 텔코 churn 데이터셋.

> 라이선스 확인 후, **prior 추출 결과(상수 테이블·하이퍼파라미터)**만 앱에 포함한다. 원본 데이터를 앱에 번들하지 않는다.

---

## 4. 예측 → 액션으로 닫기 (매출 발생 지점)

상인은 숫자가 아니라 **다음 행동**을 산다. 예측 결과를 Loop 로열티 엔진의 자동 트리거에 연결한다.

| 예측 상태 | 자동 액션 | 강도 |
|---|---|---|
| 재구매 임박 (예상시점 ±3일) | 가벼운 리마인더 + 소액 포인트 넛지 | 약 |
| 예상주기 1.5배 초과 (위험) | 윈백 쿠폰 / 보너스 포인트 자동 발송 | 중 |
| 고CLV인데 위험 | 상인에게 알림 + 강한 오퍼 제안 | 강 |

- 모든 액션은 상인이 **ON/OFF 토글**로 켜고 끈다. 기본은 OFF(동의 우선).
- **다크패턴 금지(Loop DNA)**: 넛지는 진짜 가치(포인트·실제 리마인더)여야 한다. 가짜 긴박감·재고 거짓말 금지.

---

## 5. 아키텍처 (솔로 운영 기준)

```
[Shopify 주문 웹훅] → orders 테이블 (Remix/Prisma, 기존 v1)
                              │
        (매일 새벽 1회, 스케줄 트리거)
                              ▼
                  [Python 예측 배치 워커]
        - 상점별 RFM 집계
        - 레이어(L0~L3) 선택 후 점수 산출
        - lifetimes / pymc-marketing
                              │ (점수만 write)
                              ▼
              customer_predictions 테이블
                              │
                  [Remix 로열티 엔진이 read]
              → 트리거 평가 → 액션 실행 (4장)
              → Polaris 대시보드 표시 (7장)
```

- **실시간 서빙 없음.** 야간 배치만. 운영·비용 최소.
- 워커는 별도 프로세스(예: 스케줄러 + 컨테이너). DB는 메인 앱과 공유(읽기 전용 점수 컬럼).
- 배치 실패 시: 직전 점수 유지 + 상인에게 조용히 표시("어제 기준"). 앱 기능은 절대 안 멈춤.

---

## 6. 데이터 모델 (Prisma 추가분 — CLAUDE.md에 병합)

```prisma
model CustomerPrediction {
  id                String   @id @default(cuid())
  shop              String                // 멀티테넌트 키
  shopifyCustomerId String
  pAlive            Float?                // 0~1
  expectedNextDate  DateTime?             // 다음 구매 예상시점
  predictedClv      Float?                // 향후 N개월 기대매출
  riskFlag          Boolean  @default(false)
  predictionSource  String                // "L0_prior" | "L1_shopavg" | "L2_personal" | "L3_bgnbd"
  computedAt        DateTime              // 배치 산출 시각
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([shop, shopifyCustomerId])
  @@index([shop, riskFlag])
  @@index([shop, expectedNextDate])
}

model PredictionActionLog {     // 검증·감사용
  id                String   @id @default(cuid())
  shop              String
  shopifyCustomerId String
  actionType        String                // "reminder" | "winback" | "highvalue_alert"
  triggeredAt       DateTime @default(now())
  predictedNextDate DateTime?             // 트리거 당시 예측
  actualNextOrderAt DateTime?             // 사후 채움 → 적중 추적
}
```

---

## 7. UX (Polaris, 단순함이 무기)

상인 화면은 **딱 두 줄 + 토글**. 데이터사이언스 콘솔 만들지 말 것.

```
┌────────────────────────────────────────┐
│  🔁 재구매 임박      12명   [고객보기]  │
│  ⚠️ 이탈 위험        23명   [고객보기]  │
├────────────────────────────────────────┤
│  자동 액션                              │
│   리마인더 넛지        [ ON ]           │
│   위험 고객 윈백 쿠폰   [ OFF]          │
│   고가치 고객 알림      [ OFF]          │
└────────────────────────────────────────┘
```

- 고객 상세에 "다음 구매 예상: 3월 14일경 · 신뢰도: 개인화(5회 구매 기반)" 정도만.
- P(alive) 같은 수치는 내부용. 상인에겐 임박/위험 같은 **행동 가능한 라벨**로만 노출.

---

## 8. 빌드 단계

### Phase P0 — 기반 (v2.0 직전)
- `CustomerPrediction`, `PredictionActionLog` 스키마 마이그레이션.
- orders 테이블에서 상점별 RFM 집계 쿼리.

### Phase P1 — 휴리스틱 출시 (v2.0, 가치 70%)
- L1/L2 레이어만: 본인/상점 평균 구매간격 → `expectedNextDate`, `riskFlag`.
- 카테고리 prior 상수 테이블(L0) 내장.
- Polaris 대시보드 2줄 + 리마인더 토글 1개.
- **순수 TS로 구현 가능** (평균·표준편차 수준). Python 워커 불필요.

### Phase P2 — 액션 엔진
- 트리거 규칙(4장) → 기존 로열티 포인트/쿠폰 엔진 연결.
- 윈백·고가치 알림 토글 추가. `PredictionActionLog` 기록 시작.

### Phase P3 — BG/NBD (v2.1)
- Python 배치 워커 + lifetimes/pymc-marketing.
- L3 활성화(상점 활성고객 ≥ 200). 공개데이터 prior로 부트스트랩.
- `predictedClv` 추가.

### Phase P4 — 검증 대시보드
- 9장 참조. 예측 vs 실제 적중률 추적.

---

## 9. 가드레일 (CLAUDE.md에 병합, 절대 규칙)

1. **예측을 액션으로 닫는다.** 숫자만 보여주는 화면 금지. 모든 예측은 켤 수 있는 액션이 있어야 한다.
2. **다크패턴 0.** 넛지는 실제 가치(포인트·리마인더)만. 가짜 긴박감·거짓 재고 금지. (Loop v1 원칙 계승)
3. **자동 액션 기본 OFF.** 상인 명시 동의 후에만 발송.
4. **배치 실패가 앱을 멈추지 않는다.** 점수 없으면 기능 graceful degrade.
5. **예측을 과신하지 않는다.** UI에 신뢰도(데이터 근거)를 함께 표시. 구매 1회 고객에 "확실한 이탈" 같은 단정 금지.
6. **개인정보·GDPR.** 예측 점수도 고객 데이터다. `CUSTOMERS_REDACT`·`SHOP_REDACT` 웹훅에서 `CustomerPrediction`도 함께 삭제. 클린 언인스톨 원칙 유지.

---

## 10. 검증

> 예측모델은 과거 데이터에서 잘 맞아도 실서비스에서는 으레 정확도가 떨어진다(분포 변화·신규 고객·계절성). 이를 전제로 처음부터 라이브 추적을 박는다.

- **오프라인**: 공개데이터를 시간순 holdout 분할 → 예측 expectedNextDate vs 실제 재구매. MAE(일수), P(alive) 캘리브레이션 곡선 측정.
- **라이브**: `PredictionActionLog.actualNextOrderAt`를 사후 채워 적중률 산출. "예측 임박 → 실제 구매" 전환율을, 액션 안 받은 대조군과 비교(가능하면 홀드아웃 10%). 오프라인 정확도와 라이브 정확도의 격차를 상시 모니터링한다.
- **북극성 지표**: 예측 정확도 자체가 아니라 **액션을 켠 상점의 재구매율 상승분**. 정확한 예측보다 매출을 올리는 예측이 목표다.

---

## 부록 A. 카테고리 prior 초안 (공개데이터로 갱신 전 임시값)

| 카테고리 | 중앙 재구매 주기(일) | 비고 |
|---|---|---|
| 소모품(영양제·커피·세제) | 30 | 가장 예측력 높음. 핵심 타깃 |
| 화장품·스킨케어 | 45 | |
| 식품·간식 | 21 | |
| 의류·패션 | 90 | 시즌성 큼 |
| 가전·내구재 | 365+ | 재구매 예측 가치 낮음 → 추천 중심 |

> 출시 전 Olist/Online Retail II에서 카테고리별 실측 주기로 교체.
